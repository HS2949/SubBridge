name: subbridge
description: "갤럭시 S24 Ultra 일본어 오디오 실시간 한국어 자막"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_overlay_window: ^0.5.0
  http: ^1.3.0
  permission_handler: ^11.4.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
